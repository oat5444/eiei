# คู่มือการใช้งานระบบ Password Generator (User Manual)

ยินดีต้อนรับสู่คู่มือการใช้งานระบบ **Password Generator** ระบบนี้ออกแบบมาเพื่อช่วยให้คุณสร้างรหัสผ่านที่มีความปลอดภัยสูง ยากต่อการคาดเดา และปรับแต่งได้ตามความต้องการ เพียงแค่ไม่กี่คลิก

---

## 1. ส่วนประกอบหน้าจอหลัก (Interface Overview)

หน้าจอการใช้งานถูกออกแบบมาให้เรียบง่าย โดยแบ่งออกเป็นส่วนสำคัญดังนี้:

1.  **หน้าจอแสดงผล (Display Area):** ส่วนบนสุดสำหรับแสดงรหัสผ่านที่ถูกสร้างขึ้น
2.  **ตัวชี้วัดความปลอดภัย (Strength Meter):** แถบแสดงระดับความยากของรหัสผ่าน
3.  **ส่วนกำหนดค่า (Configuration):**
    * แถบปรับความยาว (Password Length)
    * ตัวเลือกอักขระ (Character Options)
4.  **ปุ่มคำสั่ง (Action Button):** ปุ่มสำหรับสั่งให้ระบบสร้างรหัสผ่าน
5.  **ประวัติ (History):** ส่วนแสดงรหัสผ่าน 3 รายการล่าสุด

---

## 2. วิธีการกำหนดค่ารหัสผ่าน (Configuration)

ก่อนสร้างรหัสผ่าน คุณควรตั้งค่าให้ตรงกับความต้องการของเว็บไซต์หรือแอปพลิเคชันที่คุณจะนำไปใช้:

### 2.1 การปรับความยาวรหัสผ่าน (Password Length)
* เลื่อน **ปุ่มวงกลมสีน้ำเงิน** บนแถบสไลด์ไปทางซ้ายหรือขวา
* ตัวเลขทางด้านขวา (เช่น `16`) จะแสดงจำนวนตัวอักษรของรหัสผ่านที่จะสร้าง

### 2.2 การเลือกส่วนประกอบ (Character Options)
ทำเครื่องหมายถูก ($\checkmark$) ในช่องสี่เหลี่ยมหน้าตัวเลือกที่ต้องการ:
* **Include Uppercase (A-Z):** ผสมตัวอักษรพิมพ์ใหญ่
* **Include Lowercase (a-z):** ผสมตัวอักษรพิมพ์เล็ก
* **Include Numbers (0-9):** ผสมตัวเลข
* **Include Symbols (!@#$):** ผสมอักขระพิเศษ (เช่น !, @, #, $)

> **ข้อแนะนำ:** เพื่อความปลอดภัยสูงสุด (Strong) แนะนำให้เลือกครบทุกข้อ และกำหนดความยาวมากกว่า 12 ตัวอักษร

---

## 3. ขั้นตอนการสร้างและนำไปใช้ (Generating & Copying)

### ขั้นตอนที่ 1: สร้างรหัสผ่าน
คลิกที่ปุ่มสีน้ำเงินเข้มขนาดใหญ่ด้านล่างที่เขียนว่า:
**`🔄 Generate Password`**

### ขั้นตอนที่ 2: ตรวจสอบความปลอดภัย
สังเกตที่แถบ **STRENGTH**:
* ระบบจะประเมินรหัสผ่านที่ได้ว่าเป็นระดับ `WEAK` (อ่อน), `MEDIUM` (ปานกลาง), หรือ `STRONG` (แข็งแกร่ง)

### ขั้นตอนที่ 3: คัดลอกรหัสผ่าน
เมื่อได้รหัสผ่านที่ต้องการแล้ว:
1.  มองไปที่ช่องแสดงรหัสผ่านด้านบนสุด
2.  คลิกที่ **ไอคอนรูปกระดาษซ้อนกัน (Copy Icon)** ทางด้านขวาของช่อง
3.  รหัสผ่านจะถูกบันทึก (Copy) ลงใน Clipboard ของเครื่องคอมพิวเตอร์พร้อมให้คุณนำไปวาง (Paste) ได้ทันที

---

## 4. การดูประวัติย้อนหลัง (History Feature)

หากคุณเผลอกดสร้างใหม่ หรือต้องการกลับไปดูรหัสผ่านชุดก่อนหน้า ระบบจะบันทึกไว้ให้โดยอัตโนมัติ:

* ดูที่ส่วนล่างสุดของหน้าจอ หัวข้อ **"LAST 3 PASSWORDS"**
* ระบบจะแสดงรายการรหัสผ่าน 3 ชุดล่าสุดที่คุณเพิ่งกดสร้างไป

---

## 5. การแก้ไขปัญหาเบื้องต้น (Troubleshooting)

* **รหัสผ่านสั้นเกินไป:** ตรวจสอบแถบ *Password Length* ว่าไม่ได้เลื่อนไปทางซ้ายสุด
* **รหัสผ่านไม่มีตัวเลขหรือสัญลักษณ์:** ตรวจสอบว่าได้ติ๊กถูกในช่อง *Include Numbers* หรือ *Include Symbols* แล้วหรือไม่
* **กดปุ่ม Copy แล้ววางไม่ได้:** ให้ลองคลิกปุ่ม Copy ย้ำอีกครั้ง หรือลากคลุมดำที่ตัวรหัสผ่านแล้วคลิกขวาเลือก Copy

---

## Presentation_Slides
https://www.youtube.com/watch?v=0ChHdf5lRIs